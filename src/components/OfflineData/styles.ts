import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
  },
  itemContainer: {
    margin: 10,
  },
  offlineCountLabel: {
    fontSize: 16,
    fontWeight: "bold",
  },
  indicatorLabel: {
    color: "green",
  },
});
