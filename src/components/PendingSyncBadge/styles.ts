import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignSelf: "baseline",
    flexDirection: "row",
    alignItems: "center",
    gap: 4,
    backgroundColor: "#2B7FFF",
    paddingVertical: 2,
    paddingHorizontal: 4,
    borderRadius: 4,
  },
  label: {
    color: "#fff",
    fontSize: 12,
    fontWeight: "bold",
  },
});
