import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    gap: 12,
  },
  title: {
    fontSize: 18,
    fontWeight: "bold",
  },
  buildingName: {
    fontSize: 18,
  },
  dueDateLabel: {
    fontSize: 16,
  },
  dueDate: {
    fontWeight: "bold",
  },
  showToResidentLabel: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    gap: 8,
  },
});
