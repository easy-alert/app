import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  navbarContainer: {
    backgroundColor: "#fff",
  },
  loading: {
    flex: 1,
    alignContent: "center",
    justifyContent: "center",
  },
  emptyDataLabel: {
    marginTop: 20,
    textAlign: "center",
  },
});
