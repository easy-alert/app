import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    gap: 20,
  },
  footerContainer: {
    flexDirection: "row",
    gap: 10,
    justifyContent: "space-around",
  },
  footerButton: {
    flex: 1,
  },
});
